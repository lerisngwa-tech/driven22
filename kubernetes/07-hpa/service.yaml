apiVersion: v1
kind: Service
metadata:
  name: landmark-hpa-service
  namespace: landmark
spec:
  type: LoadBalancer
  selector:
    app: landmark-hpa-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
