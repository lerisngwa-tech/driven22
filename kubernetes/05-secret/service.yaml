apiVersion: v1
kind: Service
metadata:
  name: landmark-secret-service
  namespace: landmark
spec:
  type: LoadBalancer
  selector:
    app: landmark-secret-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
