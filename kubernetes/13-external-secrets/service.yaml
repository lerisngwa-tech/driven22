apiVersion: v1
kind: Service
metadata:
  name: landmark-es-service
  namespace: landmark
spec:
  type: LoadBalancer
  selector:
    app: landmark-es-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
