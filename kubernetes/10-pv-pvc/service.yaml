apiVersion: v1
kind: Service
metadata:
  name: landmark-pvc-service
  namespace: landmark
spec:
  type: LoadBalancer
  selector:
    app: landmark-pvc-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
